# Local Console UI (port 3889). Leave empty / unset only if you intentionally talk
# straight to a backend; default local debug uses Vite proxy → remote control plane.
VITE_API_BASE=http://localhost:3889

# Optional: override Vite proxy target (default https://xensemble.dev)
# VITE_DEV_PROXY_TARGET=https://xensemble.dev
