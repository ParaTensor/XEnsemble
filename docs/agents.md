# Agents

Agent 注册、`env_required`、Vault 注入与启动逻辑见 `server/src/db/index.js`、`server/src/server.js`。

**所有 UI / 交互规范以 [Designs.md](./Designs.md) 为准**（与 ParaRouter 中 `AGENTS.md` → `DESIGN.md` 的关系相同；本文不重复 Designs 内容）。
